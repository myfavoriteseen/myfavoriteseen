import { useRouter } from 'next/router'; import SceneCard from '../../components/SceneCard'; import { scenes } from '../../data/scenes'; export default function Category(){ const r = useRouter(); const slug = r.query.slug; const label = slug==='halloween'?'Halloween Looks':slug==='iconic'?'Iconic Outfits':'Props & Decor'; const filtered = scenes.filter(s => s.tags.includes(label)); return (<div className='max-w-6xl mx-auto px-6 py-12'><h1 className='text-3xl mb-6'>{label}</h1><div className='grid gap-6 md:grid-cols-2 lg:grid-cols-3'>{filtered.map(s=> <SceneCard key={s.id} scene={s} />)}</div></div>)}
