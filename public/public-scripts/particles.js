(function(){ const canvas=document.createElement('canvas'); canvas.id='particle-canvas'; document.body.appendChild(canvas); const ctx=canvas.getContext('2d'); let w,h,particles=[]; function resize(){ w=canvas.width=innerWidth; h=canvas.height=innerHeight; } window.addEventListener('resize',resize); resize(); function rand(a,b){return Math.random()*(b-a)+a} for(let i=0;i<80;i++){ particles.push({x:rand(0,innerWidth), y:rand(0,innerHeight), vx:rand(-0.25,0.25), vy:rand(-0.12,0.12), r:rand(0.5,2.8), life:rand(120,360)}) } function tick(){ ctx.clearRect(0,0,w,h); for(const p of particles){ p.x+=p.vx; p.y+=p.vy; p.life--; if(p.x<0)p.x=w; if(p.x>w)p.x=0; if(p.y<0)p.y=h; if(p.y>h)p.y=0; const g=ctx.createRadialGradient(p.x,p.y,0,p.x,p.y,p.r*20); g.addColorStop(0,'rgba(122,252,255,0.16)'); g.addColorStop(1,'rgba(122,252,255,0)'); ctx.fillStyle=g; ctx.beginPath(); ctx.arc(p.x,p.y,p.r,0,Math.PI*2); ctx.fill(); if(p.life<=0){ p.x=rand(0,w); p.y=rand(0,h); p.life=rand(120,360); p.vx=rand(-0.25,0.25); p.vy=rand(-0.12,0.12); } } requestAnimationFrame(tick);} tick(); })();
